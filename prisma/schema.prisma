// Prisma schema (Ad Library models removed on 2025-12-29 per hard-delete)
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  // Configure `DATABASE_URL` in your environment.
}
